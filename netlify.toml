# Netlify Database: Skip migrations, only generate Prisma client and build

[build]
  command = "npx prisma generate && npm run build"
  publish = ".next"
  environment = { DATABASE_URL = "${NETLIFY_DATABASE_URL}" }

[[plugins]]
  package = "@netlify/plugin-nextjs"
