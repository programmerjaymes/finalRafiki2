# PostgreSQL (Netlify Database, Neon, RDS, local Postgres, etc.)
#
# Production (Netlify + Neon):
# - Use Neon "Pooled connection" for serverless (fewer "too many connections" errors).
# - Set DATABASE_URL in Netlify → Environment variables for Production (API runtime needs it, not only build).
# - sslmode=require is auto-added for neon.tech / supabase / common cloud hosts if missing.
#
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Local: http://localhost:3000 | Production: https://your-app.vercel.app (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"
# openssl rand -base64 32 — must match between build and runtime on Vercel
NEXTAUTH_SECRET="generate-a-long-random-secret"

# --- Vercel (production) ---
# Editing .env on your laptop does NOT change the live site. In Vercel:
# Project → Settings → Environment Variables → add for "Production" (and Preview if needed):
#   NEXTAUTH_URL=https://YOUR-PROJECT.vercel.app
#   NEXTAUTH_SECRET=same-long-random-secret-as-local-or-new-one
#   DATABASE_URL=your-neon-pooled-connection-string
# Then: Deployments → Redeploy (without cache). Middleware needs NEXTAUTH_SECRET at build time.
